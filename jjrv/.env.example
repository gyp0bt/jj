# jjrv 環境変数設定
# コピーして .env.local を作成: cp .env.example .env.local

# データソース設定: "sqlite" | "neo4j"
DATA_SOURCE=sqlite

# Neo4j接続設定（DATA_SOURCE=neo4j の場合に使用）
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# JWT設定
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_IN=7d
