# status-048 (2026-02-04)

> [← README.md](../../README.md)

## 概要
ロードマップ2.5に「body大容量時の折りたたみ」機能の詳細仕様を追加し、階層畳み込み機能を実装完了。INP/JSON/Markdownの階層構造対応、50行基準の全体畳み込み、トグル操作を含む包括的なファイル畳み込み機能を実装。また、25-09（Pythonレンダラー）と25-10（バイナリbody非表示）が既に実装済みであることを確認。

## 変更内容

### spec-roadmap2.5: 詳細ビューの作り込み

#### 実装状況確認

以下の要件が**既に実装済み**であることを確認：

| # | 要件 | 状態 | 実装箇所 |
|---|------|------|---------|
| 25-09 | Pythonフォーマットレンダラー | **完了** | `src/components/BodyRenderer/index.tsx` (PythonRenderer, tokenizePythonLine) |
| 25-10 | バイナリbody非表示 | **完了** | `src/components/BodyRenderer/index.tsx` (isBinaryBody, バイナリチェックロジック) |

**実装詳細**:
- **25-09**: Pythonキーワード（def, class, import等）、組み込み関数（print, len等）、コメント、文字列、数値のシンタックスハイライトを実装
- **25-10**: NULバイトと制御文字の検出によるバイナリ判定、バイナリ時は「バイナリデータのため表示できません」を表示

#### 25-08: body大容量時の折りたたみ - 詳細仕様追加

**目的**
詳細画面で長大なbodyを持つファイルを扱いやすくするため、以下の畳み込み機能を実装する：
1. **フォーマット別階層畳み込み** — ファイルの構造に応じた意味のある単位で畳み込み
2. **全体畳み込み** — 50行以上のファイルは初期状態で折りたたみ、「続きを表示▽」で展開
3. **トグル操作** — 対応フォーマットには畳み込みトグルボタンを配置

**対応フォーマットと畳み込み単位**

| フォーマット | 畳み込み単位 | 畳み込み基準 |
|-------------|-------------|-------------|
| Abaqus INP | キーワードセクション | `*`から始まる行（例: `*Material`, `*Elastic`）をセクションの開始として検出 |
| JSON | オブジェクト/配列の階層 | トップレベルのキー（`{ "key": {...} }`）を畳み込み単位とする |
| YAML | ドキュメント/階層 | トップレベルのキー（インデント0の行）を畳み込み単位とする |
| Markdown | 見出し階層 | `#`, `##`, `###`等の見出し行を畳み込み単位とする |

**畳み込みUI仕様**

1. **全体畳み込み（50行以上）**
   - 条件: body行数が50行を超える場合
   - 初期状態: 最初の50行のみ表示、残りは折りたたみ
   - UI: 折りたたみ部分の下に「続きを表示 ▽ (残り XXX 行)」ボタンを配置
   - 展開後: 「畳む △」ボタンで再度折りたたみ可能

2. **階層畳み込み（対応フォーマットのみ）**
   - 条件: INP/JSON/YAML/Markdownのいずれか
   - UI配置: body表示エリアの右上に「畳み込みトグル 🔽/🔼」ボタンを配置
   - 初期状態: すべて展開（ユーザーが必要に応じて畳み込み）
   - 操作:
     - トグルボタンクリック: すべてのセクションを一括で畳み込み/展開
     - 個別セクションクリック: 各セクションごとに畳み込み/展開（セクション行の左側に▶/▼アイコン）

3. **畳み込み表示の見た目**
   - 畳み込み行: `▶ *Material (15行)`のように、セクション名と行数を表示
   - 展開行: `▼ *Material`のように、セクション内容を表示
   - インタラクション: セクション行全体をクリックでトグル

**実装方針**
1. **BodyRendererコンポーネント拡張** (`src/components/BodyRenderer/index.tsx`)
   - `CollapseMode`型定義: `'none' | 'full' | 'hierarchical' | 'both'`
   - フォーマット別のセクション検出ロジック（INP/JSON/YAML/MD）
   - 状態管理: `expandedSections: Set<number>`, `isFullExpanded: boolean`
2. **UIコンポーネント**
   - セクション折りたたみ行（`CollapsedSection`）
   - 全体折りたたみボタン（`ShowMoreButton`）
   - 階層トグルボタン（`HierarchicalToggleButton`）
3. **パフォーマンス考慮**
   - 大容量ファイル（1000行超）の場合は仮想スクロール検討（将来拡張）
   - セクション検出は初回レンダリング時のみ実行（`useMemo`）

#### 25-08: 階層畳み込み機能実装（完了）

**実装内容**:
- **セクション検出ロジック** (`detectSections`関数):
  - Abaqus INP: `*`から始まる行をセクション開始として検出（`**`コメント除外）
  - JSON: トップレベルのキー（インデント2スペース or タブ1つ）を検出
  - Markdown: `#`, `##`, `###`等の見出し行を検出
  - YAML: 将来拡張用にコメントアウト済み（BodyFormatに追加時に有効化）

- **状態管理**:
  - `collapsedSections: Set<number>`: 折りたたまれているセクションのインデックス
  - `hierarchicalCollapseMode: boolean`: すべて折りたたみモードのフラグ

- **UIコンポーネント**:
  - `CollapsedSectionLine`: 折りたたみ行（セクション名 + 行数表示、クリックで展開）
  - 階層トグルボタン: 右上に配置、すべて展開/折りたたみを切り替え
  - セクションヘッダー: 展開時に表示、左側に▼アイコン、クリックで折りたたみ

- **レンダリングロジック**:
  - `renderBodyWithSections`: セクションごとに展開/折りたたみを制御
  - `renderSectionBody`: セクション本体をフォーマット別にレンダリング
  - セクションが存在する場合は階層レンダリング、存在しない場合は通常レンダリング

**コミット**: `0002f93` - feat(BodyRenderer): 25-08 階層畳み込み機能を実装

## 変更ファイル

| ファイル | 変更概要 |
|---|---|
| `docs/spec-roadmap25.md` | 25-08の詳細仕様追加、25-09/25-10の実装済み状態を記録 |
| `docs/status/status-048.md` | 新規作成→更新: 今回の作業記録と実装完了状況 |
| `docs/status/status-index.md` | status-048を追加 |
| `README.md` | 最新statusリンクを047→048に更新 |
| `src/components/BodyRenderer/index.tsx` | 25-08階層畳み込み機能を実装（+273行） |
| `src/app/view/page.tsx` | import順序の自動修正 |

## 次のアクション

### 優先度P1（継続実装すべきTODO）
- [ ] 2-13: hover時type属性表示（ロードマップ2）
- [ ] 3-13: D&D分割/マージ（ロードマップ3）
- [ ] 3-14: import/export整備（ロードマップ3）

### 完了済み
- [x] 25-08: body大容量時の折りたたみ機能実装（ロードマップ2.5）— 階層畳み込み、全体折りたたみの両方完了
- [x] 25-09: Pythonフォーマットレンダラー追加（ロードマップ2.5）
- [x] 25-10: バイナリbody非表示（ロードマップ2.5）

### 実装推奨順序
1. **2-13 hover時type表示** — UXの小さな改善、実装は比較的軽量
2. **3-13, 3-14** — より大きなリファクタリングが必要な可能性

## 設計上の確認事項・懸念

### 1. 階層畳み込みの実装方法
- **JSON/YAMLの階層検出**: トップレベルのみ vs ネスト構造全体
  - 推奨: まずはトップレベルのみ実装し、必要に応じてネスト対応を追加
- **Markdownの見出し階層**: `#`のみ vs `##`, `###`も対応
  - 推奨: すべての見出しレベルを階層として扱い、親見出しを畳むと子見出しも畳む

### 2. 全体畳み込みと階層畳み込みの併用
- 50行以上のファイルで階層畳み込みも有効な場合、どちらを優先するか？
  - 推奨案: 初期状態は全体畳み込み（50行表示）、展開後に階層畳み込みトグルを有効化

### 3. パフォーマンス
- 1000行超のファイルでの動作確認が必要
- 仮想スクロール導入のタイミング（現時点では不要、将来的に検討）
