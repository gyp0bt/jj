[READMEへ戻る](../../README.md)

# services/file

グラフデータを保持しつつファイル操作と履歴管理を行う層です。`services/ssh` を使ったサーバー送受信も担当します。

## 役割
- テンプレート生成、リネーム、移動などのファイル操作。
- 操作履歴の保持（グラフデータと紐付け）。
- `services/ssh` による `get/put` の送受信。

## 方針
- `run` や `parse` と責務が被りそうでも独立させる。
- 実際のファイル操作は安全な操作単位でAPI化する。
